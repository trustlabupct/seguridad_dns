setKey("1234567890abcdef")

-- Escucha solo en interfaz local para evitar conflictos
addDOHLocal("0.0.0.0:443", "/etc/dnsdist/cert.pem", "/etc/dnsdist/key.pem")

-- Usa IP directamente en lugar de nombre si da fallo
newServer({address="172.20.0.3", name="unbound"})

-- Permitir todo el tráfico (para pruebas)
setACL("0.0.0.0/0")