server:
  listen: [0.0.0.0@53]
  identity: "knot-server"

zone:
  - domain: example.test
    file: "/zones/example.zone"
    zonefile-load: difference
    dnssec-signing: on

log:
  - target: stderr
    any: info
